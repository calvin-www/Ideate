# Prepared binary-search workspace

Import [binary-search-workspace.json](binary-search-workspace.json) using the header's Import workspace control. Export your current workspace first if you want to keep it; import asks before replacing it.

This is a clearly labeled **prepared example**, captured from browser verification. It contains the drawing, working Python, an actual recorded target-16 execution, and Gemini-authored notes whose links open the saved board and run excerpts. The conversation and edit history were removed to keep the example focused. No credentials are included.

The existing output is historical. Click Run Python for a new execution, then change the target to 17 and compare. The prepared example is useful when Gemini is slow or rate-limited; it does not simulate live AI responses.
